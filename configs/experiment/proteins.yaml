# @package _global_

defaults:
  - override /model: default.yaml
  - override /datamodule: proteins.yaml

seed: 12345

model:
  learning_rate: 0.05
  weight_decay: 0.0 #0.005
  net:
    hidden_channels: 64
    hidden_layers: 3 #2
    kappa: 3 #5
    p_dropout: 0.3
    pooling_ratio: 0.7

      

